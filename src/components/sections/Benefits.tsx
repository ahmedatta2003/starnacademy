import WhyUs from "@/components/WhyUs";
import Certificate from "@/components/Certificate";

/**
 * Benefits section: combines "Why Us" (skills, future value) and the
 * certification proof point — answering "why pick us" for parents.
 */
const Benefits = () => (
  <div id="benefits">
    <WhyUs />
    <Certificate />
  </div>
);

export default Benefits;
